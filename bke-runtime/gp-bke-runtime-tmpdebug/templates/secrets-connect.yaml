apiVersion: v1
kind: Secret
metadata:
  name: {{ printf "%s-%s" ( .Release.Name ) "connect" }}
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  username: {{ .Values.db.auth.username | b64enc | quote }}
