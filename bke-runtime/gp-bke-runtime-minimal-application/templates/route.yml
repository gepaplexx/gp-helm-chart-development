{{- if .Values.route.enabled }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "gp-bke-runtime-minimal-application.route.name" . }}
  labels:
    {{- include "gp-bke-runtime-minimal-application.labels" . | nindent 4 }}
spec:
  {{- if or (.Values.route.hostname) (.Values.route.basename)}}
  host: {{ include "gp-bke-runtime-minimal-application.route.hostname" . }}
  {{- end}}
  port:
    targetPort: http
  to:
    kind: Service
    name: {{ include "gp-bke-runtime-minimal-application.service.name" . }}
    weight: 100
  wildcardPolicy: {{ .Values.route.wildcardPolicy }}
{{- if .Values.route.tls.enabled }}
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: None
{{- end }}
{{- end }}
