kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: maven-cache
  namespace: gp-pipelines
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: rook-ceph-fs
