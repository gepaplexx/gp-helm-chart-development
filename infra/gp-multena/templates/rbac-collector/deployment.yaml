{{- if .Values.collector.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-rbac-collector
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gp-multena.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "gp-multena.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "gp-multena.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ .Release.Name }}-rbac-collector-sa
      containers:
        - name: rbac-collector
          image: {{ .Values.collector.image.name }}:{{ .Values.collector.image.tag }}
          imagePullPolicy: {{ .Values.collector.image.pullPolicy }}
          args:
            - serve
            - '--level={{ .Values.collector.logLevel }}'
            - '--cmName={{ .Values.collector.cmName }}'
            - '--cmNamespace={{ .Values.collector.cmNamespace }}'
          readinessProbe:
            httpGet:
              port: {{ .Values.collector.healthPort }}
              path: "/healthz"
            failureThreshold: 3
            periodSeconds: 2
            successThreshold: 1
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              port: {{ .Values.collector.healthPort }}
              path: "/healthz"
            failureThreshold: 2
            periodSeconds: 5
            timeoutSeconds: 10
          resources:
            requests:
              cpu: {{ .Values.collector.resources.requests.cpu }}
              memory: {{ .Values.collector.resources.requests.memory }}
            limits:
              memory: {{ .Values.collector.resources.limits.memory }}
{{- end -}}