{{- if .Values.tlsCopy.loki.enabled }}
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: copy-secret-policy-loki
spec:
  generateExisting: true
  validationFailureAction: enforce
  rules:
    - name: clone-{{ .Values.tlsCopy.loki.secretName }}-tls
      match:
        any:
          - resources:
              kinds:
                - Namespace
              names:
                - {{ .Release.Namespace }}
      generate:
        kind: Secret
        apiVersion: v1
        name: {{ .Values.tlsCopy.loki.secretName }}
        # namespace for the generated resource
        namespace: "{{`{{request.object.metadata.name}}`}}"
        synchronize: true
        clone:
          namespace: {{ .Values.tlsCopy.loki.secretNamespace }}
          name: {{ .Values.tlsCopy.loki.secretName }}
{{- end }}
{{- if .Values.tlsCopy.thanos.enabled }}
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: copy-secret-policy-thanos
spec:
  generateExisting: true
  validationFailureAction: enforce
  rules:
    - name: clone-{{ .Values.tlsCopy.thanos.secretName }}-tls
      match:
        any:
          - resources:
              kinds:
                - Namespace
              names:
                - {{ .Release.Namespace }}
      generate:
        kind: Secret
        apiVersion: v1
        name: {{ .Values.tlsCopy.thanos.secretName }}
        # namespace for the generated resource
        namespace: "{{`{{request.object.metadata.name}}`}}"
        synchronize: true
        clone:
          namespace: {{ .Values.tlsCopy.thanos.secretNamespace }}
          name: {{ .Values.tlsCopy.thanos.secretName }}
{{- end }}