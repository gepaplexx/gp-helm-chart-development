apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "gp-multena-instance.fullname" . }}-groups
  labels:
    {{- include "gp-multena-instance.labels" . | nindent 4 }}
data:
  groups.yaml: |-
    {{- .Values.config | nindent 4 }}
      groups:
        - gepaplexx: [gepaplexx]
        - gepardec: [Uff, blubb]