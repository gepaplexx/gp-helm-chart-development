apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "gp-multena-instance.fullname" . }}-users
  labels:
    {{- include "gp-multena-instance.labels" . | nindent 4 }}
data:
  users.yaml: |-
      users:
        - lucostus: [lucostus]
        - uff: [dev,dev2]