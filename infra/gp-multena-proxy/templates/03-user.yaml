apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "gp-multena-instance.fullname" . }}-user
  labels:
    {{- include "gp-multena-instance.labels" . | nindent 4 }}
data:
  user.yaml: |-
      users:
        - lucostus: [lucostus]
        - uff: [dev,dev2]