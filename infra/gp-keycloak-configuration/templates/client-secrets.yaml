apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: client-secrets
  namespace: {{ .Release.Namespace }}
spec:
  # Einträge für ARGO_WF_CLIENT_SECRET, VAULT_CLIENT_SECRET, OCP_IP_CLIENT_SECRET, GRAFANA_CLIENT_SECRET
  dataFrom:
    - extract:
        key: #TODO in vault definieren und einfügen. Hier Pfad aktualisieren
        conversionStrategy: Default
        decodingStrategy: None
  refreshInterval: 12h
  secretStoreRef:
    kind: ClusterSecretStore
    name: #TODO in vault definieren und einfügen. Hier Pfad aktualisieren
  target:
    creationPolicy: Owner
    deletionPolicy: Retain
    name: client-secrets
    template:
      engineVersion: v2
      metadata:
        labels:
          app.kubernetes.io/managed-by: client-secrets