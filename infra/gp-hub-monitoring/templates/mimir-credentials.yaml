apiVersion: v1
kind: Secret
metadata:
  name: mimir-basic-auth-credentials
  namespace: {{ .Release.Namespace }}
data:
  username: {{ .Values.mimir.nginx.basicAuth.username | b64enc }}
  password: {{ .Values.mimir.nginx.basicAuth.password | b64enc }}