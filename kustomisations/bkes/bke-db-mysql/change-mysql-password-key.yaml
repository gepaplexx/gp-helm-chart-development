apiVersion: gepaplexx.com
kind: SedTransformer
metadata:
  name: replace-foo-with-bar
  annotations:
    config.kubernetes.io/function: |
      exec:
        path: sedtransformer.sh
spec:
  replacements:
    mysql-password: password
  debug: false
