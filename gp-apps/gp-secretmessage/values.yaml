# Default values for gp-secretmessage.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
route:
  hostname: ""

ports:
  http: 8080
  https: 8443

secure:
  redirectEnabled: "true"
  tlsAutoDomain: "example.com"


