apiVersion: v1
kind: Service
metadata:
  name: {{ include "gp-bke-deploy-app.fullname" . }}
  labels:
    {{- include "gp-bke-deploy-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gp-bke-deploy-app.selectorLabels" . | nindent 4 }}
