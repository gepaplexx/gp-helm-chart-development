kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ .Values.apache.htdocsPVC }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.pvc.size }}
  storageClassName: ceph-filesystem